import styled from 'styled-components';

const UserHeaderStyle = styled.div`
  display: flex;
  align-items: center;
  .profile-photo {
    margin-right: 15px;
  }
  .profile-name {
    font-weight: 700;
    font-size: 1.25rem;
  }
`;

export default UserHeaderStyle;
